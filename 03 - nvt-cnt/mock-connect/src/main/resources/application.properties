# =============================================================================
# application.properties - Fallback local da aplicação mock-connect
# =============================================================================
# Usado quando o Config Server não está disponível (ex.: rodar fora do K8s).
# No cluster, o Config Server entrega server.port e server.servlet.context-path
# via connect.properties + connect-kind.properties; estes valores servem como
# padrão para desenvolvimento local ou se o config client falhar.
# =============================================================================
server.port=9090
server.servlet.context-path=/portal
