# =============================================================================
# namespace.yaml - Namespace do lab (aplicação de exemplo no Kind)
# =============================================================================
# Isola todos os recursos do lab (Config Server, portal-demo, ConfigMap) em um
# namespace próprio (lab-portal). Deve ser aplicado antes dos outros manifestos.
# =============================================================================
apiVersion: v1
kind: Namespace
metadata:
  name: lab-portal
  labels:
    app.kubernetes.io/name: lab-portal
